apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-app
  labels:
    app: web-app
  annotations:
    container.apparmor.security.beta.kubernetes.io/web-app: unconfined
spec:
  replicas: 1
  selector:
    matchLabels:
      app: web-app
  template:
    metadata:
      labels:
        app: web-app
    spec:
      volumes:
      - name: host-vol
        hostPath:
          path: /etc
      containers:
      - name: web-app
        image: nginx:latest
        ports:
        - containerPort: 80
          hostPort: 80
        resources:
          requests:
            memory: "300Mi"
            cpu: "500m"
          limits:
            memory: "2800Mi"
            cpu: "5000m"
        securityContext:
          privileged: true
          capabilities:
            add:
            - SYS_ADMIN
